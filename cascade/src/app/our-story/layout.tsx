import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Our Story | Ripplecast",
};

export default function OurStoryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
